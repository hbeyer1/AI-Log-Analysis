You are filtering a list of extracted conversation objectives. Your job: for each objective, decide whether it matches any of the EXCLUSION PATTERNS below. Excluded objectives are non-meaningful (greetings, test messages, trivial exchanges) and should not enter the taxonomy.

<exclusion_patterns>
{patterns_block}
</exclusion_patterns>

<objectives>
{objectives_block}
</objectives>

<instructions>
For each objective, decide:
- "keep" — the objective is substantive and none of the exclusion patterns apply, OR
- "exclude" — the objective matches one of the exclusion patterns.

Match generously. If an objective is borderline but clearly captured by a pattern's intent, exclude it.

Be strict about NOT excluding substantive objectives just because they are short or simple.
</instructions>

<output_format>
Return a JSON array with one object per objective, in the same order:

{
  "index": <1-based index matching the objectives list above>,
  "decision": "keep" | "exclude",
  "matched_pattern": "<id of matched exclusion pattern if decision is exclude, else empty string>",
  "reason": "<1 short sentence explaining the decision>"
}

Return ONLY the JSON array. No prose, no markdown fences.
</output_format>
